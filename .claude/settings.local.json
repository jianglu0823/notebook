{
  "permissions": {
    "allow": [
      "Bash(git commit -m ' *)",
      "Bash(docker compose up -d --build app:*)",
      "Bash(set -a && . ./.env && set +a && docker compose up -d --build app:*)"
    ]
  },
  "enabledPlugins": {
    "spec-superflow@spec-superflow": true
  }
}
